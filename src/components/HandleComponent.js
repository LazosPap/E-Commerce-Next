const HandleComponent = () => {
  // We create the dots on the resize of rnd at the edges
  return (
    <div
      className="w-5 h-5 rounded-full shadow border bg-white
    border-zinc-200 transition hover:bg-primary"
    />
  );
};

export default HandleComponent;
